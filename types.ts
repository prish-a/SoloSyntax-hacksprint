export interface InputFieldProps {
  label: string;
  type: string;
  placeholder: string;
  id: string;
}

export interface SocialSignInButtonProps {
  icon: string;
  provider: string;
  alt: string;
}
